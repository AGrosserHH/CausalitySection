"""Reviewed, private-use causal comparisons; reuses the private-workspace ownership model."""
# Recorded inside run payloads and exported bundles: a format label, so keep the value stable.
ANALYSIS_VERSION = "0.1.0-p1.1"
