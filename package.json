{
  "name": "causality-section",
  "private": true,
  "scripts": {
    "dev": "npm --prefix CausalGUI/causal-frontend run dev",
    "lint": "npm --prefix CausalGUI/causal-frontend run lint",
    "test": "npm --prefix CausalGUI/causal-frontend run test",
    "build": "npm --prefix CausalGUI/causal-frontend run build",
    "format": "npm --prefix CausalGUI/causal-frontend run format"
  }
}
