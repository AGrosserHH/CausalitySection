{
  "name": "causality-section",
  "private": true,
  "scripts": {
    "dev": "npm --prefix aitiolin/causal-frontend run dev",
    "lint": "npm --prefix aitiolin/causal-frontend run lint",
    "test": "npm --prefix aitiolin/causal-frontend run test",
    "build": "npm --prefix aitiolin/causal-frontend run build",
    "format": "npm --prefix aitiolin/causal-frontend run format"
  }
}
